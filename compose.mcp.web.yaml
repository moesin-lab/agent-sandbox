services:
  mcp-web:
    build:
      context: ./mcp
    image: ${MCP_BASE_IMAGE:-agent-sandbox-mcp:dev}
    command: ["node", "services/web/server.js"]
    networks:
      - agent_net
      - egress_net

networks:
  agent_net:
    external: false
  egress_net:
    external: false
